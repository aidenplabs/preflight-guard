Read README.md and context.md first.

We are starting v2 now.
Do NOT rebuild the project from scratch.
Build on top of the current v1 codebase and preserve its narrow philosophy.

Before changing anything:
- summarize the current v1 architecture
- summarize the current limitations
- explain exactly what belongs in v2 and what still does NOT belong yet

v2 goal:
Improve the tool in a practical, still-narrow way without turning it into a generic security scanner.

v2 priorities:
1. improve stack-aware detection quality
2. improve rule organization for future extensibility
3. improve finding quality and reduce false positives / false negatives
4. keep the same CLI-first shape
5. keep beginner-friendly ship / caution / no-ship reporting

What v2 SHOULD include:
- introduce a cleaner internal structure for future rule packs or stack profiles, but keep it simple
- improve the current Next.js + Supabase + Vercel profile quality
- refine existing rules using better project/context signals
- improve confidence handling
- improve report usefulness
- optionally add a small number of CLI flags only if they directly help real usage

Good v2 candidate areas:
- better stack/profile confidence reporting
- cleaner rule metadata and grouping
- better route sensitivity heuristics
- better separation of confirmed issue vs likely issue vs review-needed
- better output structure for future GitHub Action integration
- tuning based on real sample repo behavior if possible

What v2 should NOT include:
- support for many frameworks
- live cloud scanning
- dynamic runtime testing
- dashboard
- SaaS backend
- auth/accounts/billing/team features
- enterprise policy engine
- broad SAST ambitions
- v3 ideas

Working style:
- keep scope tight
- avoid overbuilding
- do not add features just because they sound nice
- explain why each v2 change is worth it
- prefer a smaller strong v2 over a bigger messy one

Deliverables:
1. proposed v2 scope
2. files to change
3. implementation plan
4. actual code changes
5. README/context updates if needed
6. final summary of what improved from v1 to v2

Also update context.md with:
- v2 scope
- design decisions
- files changed
- remaining risks
- what should wait for v3
